{
  // ───────── Clipboard ─────────
  "custom/clipboard": {
    "format": "C",
    "tooltip": "Clipboard History",
    "on-click": "$HOME/.config/waybar/scripts/clipboard.sh --show",
    "on-click-right": "$HOME/.config/waybar/scripts/clipboard.sh --clear"
  }
}
