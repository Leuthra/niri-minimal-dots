[Journal]
# Store logs persistently on disk instead of holding them in RAM tmpfs
Storage=persistent

# Cap RAM usage in /run/log/journal
RuntimeMaxUse=30M

# Cap disk usage in /var/log/journal
SystemMaxUse=100M

# Automatically purge logs older than 1 month
MaxRetentionSec=1month

# Enable compression
Compress=yes
